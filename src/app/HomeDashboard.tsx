/**
 * Kok route (/) sahibi. Ileride hos geldin veya bos durum metni burada genisletilebilir.
 */
export function HomeDashboard() {
  return <section className="home-dashboard" aria-label="Ana ekran" />;
}
